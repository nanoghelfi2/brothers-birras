function add(delta) {
    valor = eval(formulario.cantidad.value);
    formulario.cantidad.value = eval(valor+delta);
    }

function addDesk(delta) {
    valor = eval(formularioDesk.cantidadDesk.value);
    formularioDesk.cantidadDesk.value = eval(valor+delta);
    }

